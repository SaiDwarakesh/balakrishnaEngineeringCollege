import { SmartCellDirective } from './smart-cell.directive';

describe('SmartCellDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartCellDirective();
    expect(directive).toBeTruthy();
  });
});
